services:
  net-bot:
    build: .
    restart: unless-stopped
    volumes:
      - ./config.yaml:/app/config.yaml
      - ./data:/app/data
    # If corescope is on the same host (not in Docker), uncomment:
    # network_mode: host
