FROM python:3.12-slim

WORKDIR /app

COPY relay_requirements.txt .
RUN pip install --no-cache-dir -r relay_requirements.txt

COPY relay_service.py .

CMD ["uvicorn", "relay_service:app", "--host", "0.0.0.0", "--port", "8080"]
