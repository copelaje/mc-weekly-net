services:
  relay:
    build:
      context: .
      dockerfile: Dockerfile.relay
    restart: unless-stopped
    environment:
      - RELAY_TOKEN=${RELAY_TOKEN}
      - COMPANION_HOST=${COMPANION_HOST}
      - COMPANION_PORT=${COMPANION_PORT:-4403}
